% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_read_regions_bed.R
\name{.read_regions_bed}
\alias{.read_regions_bed}
\title{Read and normalize a regions BED-like file}
\usage{
.read_regions_bed(
  regions_bed,
  region_seq_col = NULL,
  region_start_col = NULL,
  region_end_col = NULL,
  region_name_col = NULL,
  regions_coord = c("bed0", "gff1"),
  stop_on_invalid = TRUE,
  ...
)
}
\description{
Returns a data.frame with columns: seqname, start, end, region_name
}
\details{
Robust to headerless BED files: tries header=TRUE, then falls back to header=FALSE.

Coordinate conventions:
\itemize{
\item regions_coord = "bed0": interpret as BED 0-based, half-open interval
(\code{start}, \code{end}), and convert to 1-based, closed interval (\code{start + 1}, \code{end})
for overlap with GFF-like coordinates.
\item regions_coord = "gff1": interpret as 1-based, closed interval (\code{start}, \code{end}).
}

Column selectors:
\itemize{
\item region_*_col may be NULL (default), a column \emph{name}, or a 1-based \emph{integer index}.
For headerless input, indices (1,2,3,4) are often easiest.
}
}
\keyword{internal}
