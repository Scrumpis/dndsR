% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_filter_dnds.R
\name{.filter_dnds}
\alias{.filter_dnds}
\title{Filter a dN/dS annotation table}
\usage{
.filter_dnds(d, dnds_col = "dNdS", filter_expr = NULL, max_dnds = 10, ...)
}
\arguments{
\item{d}{A data.frame containing a dN/dS column.}

\item{dnds_col}{Character. Name of the column containing dN/dS values.
Default is \code{"dNdS"}.}

\item{filter_expr}{Optional character string representing a logical
expression evaluated within the data frame (e.g. \code{"gene_type == 'CDS'"}).
If it evaluates to length 1 it is recycled; if it evaluates to length
\code{nrow(d)} it is used directly. Any other result or errors cause a
warning and the expression is ignored.}

\item{max_dnds}{Numeric. Rows with dN/dS values greater than or equal
to this threshold are removed. Default is \code{10}.}

\item{...}{Additional arguments (currently unused; included for
forward compatibility).}
}
\value{
A filtered \code{data.frame}.
}
\description{
Internal helper used across dndsR to standardize filtering of dN/dS
annotation tables. Rows are retained if:
\enumerate{
\item The specified dN/dS column is finite.
\item The value is strictly less than \code{max_dnds}.
\item (Optional) The row satisfies an additional logical expression
supplied via \code{filter_expr}.
}
}
\details{
If \code{filter_expr} fails to parse/evaluate or returns a non-logical / wrong-length
result, it will be ignored with a warning (rather than stopping).
}
\keyword{internal}
