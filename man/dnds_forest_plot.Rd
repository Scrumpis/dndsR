% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dnds_forest_plot.R
\name{dnds_forest_plot}
\alias{dnds_forest_plot}
\title{Forest plot from dnds_contrast summary TSVs}
\usage{
dnds_forest_plot(
  input,
  pattern = "_dnds_contrast\\\\.tsv$",
  recursive = TRUE,
  output_dir = NULL,
  file_prefix = "contrast_forest_plot",
  facet = c("auto", "none", "mode", "side_tag", "both"),
  order_by = c("mean_delta", "contrast_name")
)
}
\arguments{
\item{input}{Path to a directory OR a character vector of TSV file paths.
If a directory, files are discovered recursively (optional) by pattern.}

\item{pattern}{Regex used to match summary TSV filenames.}

\item{recursive}{Logical; if TRUE and input is a directory, search recursively.}

\item{output_dir}{Where to write outputs. Default: if input is a directory, that directory;
otherwise dirname of the first file.}

\item{file_prefix}{Prefix for output files (PDF/TSV).}

\item{facet}{One of "auto", "none", "mode", "side_tag", "both".}

\item{order_by}{Ordering of rows in the plot: "mean_delta" or "contrast_name".}
}
\value{
Invisibly returns a list with paths: plot_pdf, table_tsv, n_rows.
}
\description{
Reads one or more *_dnds_contrast.tsv summary files (produced by dnds_contrast)
and generates a forest plot of mean_delta with confidence intervals.
}
\details{
Typical use: run after stats are already computed.
}
