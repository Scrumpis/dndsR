% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_comparisons.R
\name{split_comparisons_by_label}
\alias{split_comparisons_by_label}
\title{Split genomes by subgenome / haplotype / custom and emit a new comparison_file}
\usage{
split_comparisons_by_label(
  comparison_file,
  mode = c("subgenome", "haplotype", "custom"),
  custom_regex = NULL,
  case_insensitive = TRUE
)
}
\arguments{
\item{comparison_file}{Path to whitespace-delimited file (tabs/spaces; header or not)
with columns: comparison_name, query_fasta, query_gff, subject_fasta, subject_gff.}

\item{mode}{One of "subgenome", "haplotype", or "custom".}

\item{custom_regex}{Used only when mode="custom". A regex with EXACTLY ONE capture group
that extracts the label from sequence names / GFF seqids (e.g. "_hap(\\d+)$", "(A|B|C)$").}

\item{case_insensitive}{Logical; match labels case-insensitively (default TRUE).}
}
\value{
Invisibly, the path to the new comparison file (with "_split" inserted before the extension).
}
\description{
Split genomes by subgenome / haplotype / custom and emit a new comparison_file
}
